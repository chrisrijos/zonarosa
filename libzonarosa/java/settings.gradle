pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = 'libzonarosa'

include 'client', 'server', 'backup-tool'

if (!hasProperty('skipAndroid')) {
    include ':android', ':android:benchmarks', ':android:packaging-test'
}
