plugins {
    id 'zonarosa-sample-app'
    alias(libs.plugins.compose.compiler)
}

android {
    namespace 'io.zonarosa.contactstest'

    defaultConfig {
        applicationId "io.zonarosa.contactstest"
    }
}

dependencies {
    implementation project(':lib:contacts')
}