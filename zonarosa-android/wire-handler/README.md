# Build

`./gradlew build`

# Move

`mv lib/build/libs/wire-handler-1.0.0.jar .`

# Update ZonaRosa project

If version or name changed, update `build.gradle` with new wire-handler jar path in `buildScript/dependencies`
